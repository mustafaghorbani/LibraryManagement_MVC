@model BookBorrowModel

<div class="col-md-12 page-header">
    Borrow book:
</div>

<form class="form-group card p-3 bg-light" method="post" asp-controller="Home" asp-action="Borrow">
    <div class="row mb-2">
        <div class="col">@Html.TextBoxFor(model => model.ISBN, new { @class = "form-control",@placeHolder="ISBN" })</div>
        <div class="col">@Html.TextBoxFor(model => model.MemberId, new { @class = "form-control",@placeHolder="Memnber Id" })</div>
    </div>
    <button type="submit" class="btn btn-primary" style="float: right;">Save</button>
</form>

<!-- Modal -->
@*<div class="modal fade" id="borrowModal" tabindex="-1" role="dialog" aria-labelledby="borrowModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Borrow Book</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form class="form-group card p-3 bg-light col-6" method="post" asp-controller="Home" asp-action="Borrow">
                    @Html.TextBoxFor(model => model.ISBN, new { @class = "form-control",@placeHolder=Model.ISBN, @disabled:"disabled" })
                    @Html.TextBoxFor(model => model.MemnberId, new { @class = "form-control",@placeHolder="Memnber Id" })
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-primary" style="float: right;">Save</button>
            </div>
             </form>
        </div>
    </div>
</div>*@